name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Build release assets
        shell: bash
        run: |
          set -euo pipefail

          version="${GITHUB_REF_NAME}"
          mkdir -p dist

          while read -r target_os target_arch; do
            output="dist/aircom_${version}_${target_os}_${target_arch}"
            CGO_ENABLED=0 GOOS="${target_os}" GOARCH="${target_arch}" \
              go build -trimpath -ldflags "-X main.version=${version}" \
              -o "${output}" ./cmd/aircom
          done <<'TARGETS'
          darwin arm64
          darwin amd64
          linux amd64
          linux arm64
          TARGETS

          "dist/aircom_${version}_linux_amd64" --version | grep -Fx "aircom ${version}"

          (
            cd dist
            sha256sum aircom_* > SHA256SUMS
          )

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            dist/SHA256SUMS dist/aircom_* \
            --verify-tag \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes \
            --latest=false
